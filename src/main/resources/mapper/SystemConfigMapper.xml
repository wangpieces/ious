<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wangpiece.ious.mapper.SystemConfigMapper">

    <select id="getSystemConfigList" parameterType="Integer" resultType="SystemConfig">
        select * from system_config where type = #{type};
    </select>

</mapper>